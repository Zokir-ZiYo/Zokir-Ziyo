https://codepen.io/hafeez-pp/pen/XWjRqex
https://codepen.io/RedJokingInn/pen/pVKWjz
https://demo.webslesson.info/excel-to-html-javascript/
excel read html table js
Биринчи автоматга Ввод -1  уртасига I-с.ш 0.4кВ. Уртадаги автоматга СВ Иккинчи автоматга Ввод-2 уртасига  св автоматдан кейин II-с.ш 0.4кВ.
#include <SPI.h>
#include <Ethernet2.h>
int buttonState = 0;

byte mac[] = {
  0xDE, 0xAD, 0xBE, 0xEF, 0xFE, 0xED
};
IPAddress ip(192, 168, 0, 106);
EthernetServer server(80);
void setup() {
  Serial.begin(9600);
  while (!Serial) {
    ;
  }
  Ethernet.begin(mac, ip);
  server.begin();
  Serial.print("server is at ");
  Serial.println(Ethernet.localIP());
  pinMode(6,INPUT);
}

void loop() {
  buttonState = digitalRead(6);
  
  EthernetClient client = server.available();
       {
              if (buttonState == HIGH) {
            client.println("AVARIYA!!!");}
          else
            {
            client.println("Hammasi joyida");}
          }
  client.stop(); 
  
}
